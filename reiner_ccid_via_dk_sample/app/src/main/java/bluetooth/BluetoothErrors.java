/*
* 
*   
* 
*   Created by Steve Spormann on 5.5.2015.
*   Copyright (c) 2015 REINER SCT. All rights reserved.
* 
*   Version:    0.5.3
*   Date:       1.03.2017
*   Autor:      S. Spormann
*   eMail:      mobile-sdk@reiner-sct.com
*/
package bluetooth;

// TODO: Auto-generated Javadoc
/**
 * The Enum BluetoothErrors.
 */
public enum BluetoothErrors {
    
        /** The Connection ended unexpected. */
        ConnectionEndedUnexpected,
        
        /** The Transaction canceled by user. */
        TransactionCanceledByUser,
        
        /** The No reader found in shared preferances. */
        NoReaderFoundInSharedPreferances,
        
        /** The No reader bonded in android system. */
        NoReaderBondedInAndroidSystem,
        
        /** The No reader found. */
        NoReaderFound,
        
        /** The Bluetooth pairing corrupted. */
        BluetoothPairingCorrupted,
        
        /** The Bluetooth pairing failed. */
        BluetoothPairingFailed,
        
        /** The Card related error. */
        CardRelatedError,
        
        /** The No reader set. */
        NoReaderSet,
        
        /** The Did disconnect unexpected. */
        DidDisconnectUnexpected,
        
        /** The Transaction was canceled by the user. */
        TransactionWasCanceledByTheUser,
        
        /** The Protocoll error. */
        ProtocollError,
        
        /** The Bluetooth le not supported. */
        BluetoothLENotSupported,
        
        /** The Could not connect to device. */
        CouldNotConnectToDevice
}
